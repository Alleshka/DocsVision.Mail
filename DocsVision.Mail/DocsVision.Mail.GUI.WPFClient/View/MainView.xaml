<UserControl x:Class="DocsVision.Mail.GUI.WPFClient.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DocsVision.Mail.GUI.WPFClient.View">
    <GroupBox Header="Почта">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="3"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="2"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="5*"/>
            </Grid.ColumnDefinitions>
            <Button Command="{Binding LogOut}" Grid.Row="1" Grid.Column="2" Content="Выход"/>
            <CheckBox Grid.Row="1" Grid.Column="0" IsChecked="{Binding IsCheckBox}">
                <TextBlock>Отправка сообщений</TextBlock>
            </CheckBox>
            <ContentPresenter Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Content="{Binding CurViewModel}"></ContentPresenter>
        </Grid>
    </GroupBox>
</UserControl>
